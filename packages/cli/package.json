{
  "name": "@shdr/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "shdr": "./dist/bin.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "pretest": "tsdown",
    "check": "tsc --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --root ."
  },
  "dependencies": {
    "@shdr/core": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^24.13.3",
    "tsdown": "^0.22.5",
    "typescript": "7.0.2"
  }
}
